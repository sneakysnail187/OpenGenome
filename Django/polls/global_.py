submittedCSVFileNames = ['test'] 

def fileNames(names):
    return names
#/submittedCSV/csvFile.name --> add /uploads or whatever for the url path in analysis, 
#then we take the index chosen from the dropdown bar on the fetch request as an addition to the localhost